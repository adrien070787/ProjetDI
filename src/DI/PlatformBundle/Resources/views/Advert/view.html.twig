{% extends "DIPlatformBundle::layout.html.twig" %}

{% block title %}Lecture d'une annonce - {{ parent() }} {% endblock %}

{% block diplatform_body %}
    <h3>{{ annonce.title }}</h3>
    <i>Par {{ annonce.author }}, le {{ annonce.date|date('d/m/Y') }}</i>

    <div>{{ annonce.content }}</div>

    <div>
        <a href="{{ path('di_platform_home') }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à la liste
        </a>
        <a href="{{ path('di_platform_edit', {'id': annonce.id }) }}" class="btn btn-default">
            <i class="glyphicon glyphicon-edit"></i>
            Modifier l'annonce
        </a>
        <a href="" class="btn btn-danger">
            <i class="glyphicon glyphicon-trash"></i>
            Supprimer l'annonce
        </a>
    </div>

{% endblock %}